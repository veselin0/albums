import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Card = () => {
  return (
    <View></View>
  );
};

const style = StyleSheet.create({});

export default Card;
